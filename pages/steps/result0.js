if (typeof window !== 'undefined') {
    // Perform localStorage action
    const items = sessionStorage.getItem('report0Items')
  }


export default function Result0 () {



    return ( 
    <div>Hello</div>

  )
}